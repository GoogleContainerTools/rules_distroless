schemaVersion: "2.0.0"

commandTests:
  - name: "apt list --installed"
    command: "apt"
    args: ["list", "--installed"]
    expectedOutput:
      - Listing\.\.\.
      - nvidia-container-toolkit-base/now 1.16.1-1 arm64 \[installed,local\]
